<div class="top">
    <div class="header">
        <div class="header_icon">
            <h1 class="icon">Daita</h1>
        </div>
        <div class="devise">
            <div class="select-boxes">
                <% if user_signed_in?  %>
                  <%= link_to current_user.nickname, '#', class: "select-box" %>
                  <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "select-box" %> 
                <% elsif %> 
                  <%= link_to "ログイン", new_user_session_path, class: "select-box" %>
                  <%= link_to "新規登録", new_user_registration_path, class: "select-box" %> 
                <% end %> 
            </div>
        </div>
    </div>
    <div class="main">
        <div class="search_new_judgment_box">
            <div class="search">
                <%= form_with(url: '#', local: true, method: :get, class: "search-form") do |form| %>
                  <%= form.text_field :keyword, placeholder: "議題の検索", class: "search-input" %>
                <% end %>
            </div>
            <div class="judgment_box">
                <%= link_to "投稿", new_agendae_path, class: "new_judgment"%>
            </div>
        </div>
        <div class="catch_copy_box">
            <h1 class="catch_copy_sentence">あなたの基準合ってますか？</h1>
            <p>基準を知ることで、「自信・成長・安心」が生まれ、選択肢が広がる。</p>
        </div>  
        <div class="agenda_list_box">
            <div class="agenda_explain">
                <h3>議題一覧</h3>
            </div>
            <% @agenda.each do |agenda| %>
                <div class="agenda_list">
                    <div class="agenda_title">
                        <%= agenda.title %>
                    </div>
                    <div class="agenda_overview">
                        <%= agenda.catch_copy %>
                    </div>
                    <div class="border_bottom"></div>
                </div>
            <% end %>
        </div>
    </div>
    <div class="footer">
    </div>
</div>

